#include<stdio.h>
int main()
{
    int arr[5] = {2, 6, 100, 1, 18};
    int min = arr[0];

    for(int i = 1; i < 5; i++)
    {
        if(arr[i] < min)
        {
            min = arr[i];
        }
    }

    printf("Minimum number in the array is: %d\n", min);

    return 0;
