#include<stdio.h>

int main()
{
    int arr[5] = {2, 6, 100, 14, 18};
    int max = arr[0];

    for(int i = 1; i < 5; i++)
    {
        if(arr[i] > max)
        {
            max = arr[i];
        }
    }

    printf("Maximum number in the array is: %d\n", max);

    return 0;
}
